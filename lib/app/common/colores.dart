import 'package:flutter/material.dart';

class Colores {
  /// Color verde usado como accent color
  static const Color primaryColor = Color(0xFF09c273);

  /// Color verde usado como accent color
  static const Color accentColor = Color(0XFF1ABC9C);

  ///Primer color del gradiente para los formularios
  static const Color gradient1 = Color(0xFF09c273);

  /// Segundo color del gradient para los formularios
  static const Color gradient2 = Color(0xFF05b386);

  /// Color utilizado para los iconos que indican un alerta
  static const Color warning = Color(0xFFee712a);

  /// Color utilizado para el widget Divider
  static const Color divider = Color(0xFF6d7480);

  /// Color utilizado para el título de los formularios
  static const Color TituloFormulario = Color(0xFF69949C);

  /// Color utilizado el texto que acompaña un Radio button o Check box
  static const Color radio_chechbox = Color(0xFF75808f);

  /// Color utilizado para los mensajes de error de los inputs
  static const Color errorTexto = Color(0xFFD32F2F);
}
